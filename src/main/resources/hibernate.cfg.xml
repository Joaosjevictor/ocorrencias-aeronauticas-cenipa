<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Configurações de Conexão -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/OcorrenciasAeronauticas</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">ifpb</property>

        <!-- Dialeto do Banco -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Mostra o SQL no console -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Atualiza o esquema automaticamente -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapeamento das classes -->
        <mapping class="br.edu.faculdade.projeto.model.Localidade"/>
        <mapping class="br.edu.faculdade.projeto.model.Ocorrencia"/>
        
    </session-factory>
</hibernate-configuration>